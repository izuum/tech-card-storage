services:
  postgres_db:
    image: postgres:latest
    restart: always
    shm_size: 128mb
    environment:
      SPRING_PROFILES_ACTIVE: default
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
    driver: local